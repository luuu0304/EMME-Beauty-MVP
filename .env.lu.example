# Plantilla para Lu — misma password que estaba hardcodeada en index.js.
# No hace falta recrear el contenedor ni perder los datos de prueba.
#
#   cp .env.lu.example .env

# --- Backend ---
PORT=3000

# --- SQL Server (Docker + backend) ---
DB_USER=sa
DB_PASSWORD=TThmA4bmPfPUk*
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=EmmE_Beauty
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true
DB_USE_UTC=false
